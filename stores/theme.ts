// Theme store removed — useThemeStore was never imported or called anywhere.
// The dark theme is the only theme (CSS variables only define dark mode colors).
// If theme switching is needed in the future, recreate this store with proper
// CSS variable switching and cookie/localStorage persistence.
export {}
